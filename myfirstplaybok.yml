---
- host: all
  become: yes
  tasks:
    - name: i want to ping 
      ping:

    - name: Update repositories cache and install "apache2" package
      apt:
       name: apache2
      state: present