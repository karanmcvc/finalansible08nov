---
- host: all 
  become: yes
  tasks: 
  - name: Install the package "apache2"
  apt:
    name: apache2
    state: present 

- name: i want to start my webserver
  service:
     name: apache2
     state: started